package com.julianotalora.features.countriesuiartifact.ui.views

import androidx.compose.runtime.Composable

@Composable
fun CountryDetailsView(
    //state: Result<CountryDetails>,
    //onEvent: (Event) -> Unit
) {
    //when (state) {
        /*
        is Result.Loading -> {
            androidx.compose.material3.CircularProgressIndicator()
        }
        is Result.Success -> {
            val details = state.data
            androidx.compose.material3.Text(text = "Country: ${details.nameCommon}")
            // Add more UI components to display details as needed
        }
        is Result.Error -> {
            androidx.compose.material3.Text(text = "Error: ${state.error}")
        }
        */
    //}
}